const ScheduleDetailContents = ({
  children,
}: {
  children: React.ReactNode;
}) => {
  return <div className='ml-5 flex flex-col gap-4'>{children}</div>;
};

export default ScheduleDetailContents;
